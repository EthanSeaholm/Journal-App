/**
 * This interface describes the structure of a User object. It ensures each user will have the specified properties.
 * The properties of a User include a unique username and an email.
 */

export interface User {
    username: string,
    email: string,
}